{
  "name": "smed-changeover-manager",
  "version": "1.0.0",
  "private": true,
  "engines": { "node": ">=18" },
  "scripts": {
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "db:generate": "npm run generate --prefix server",
    "db:migrate": "npm run migrate --prefix server",
    "db:seed": "npm run seed --prefix server",
    "db:reset": "npm run db:reset --prefix server",
    "build": "npm --prefix client install && npm --prefix client run build && npm --prefix server install && npm --prefix server run generate",
    "start": "NODE_ENV=production node server/scripts/startProd.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
